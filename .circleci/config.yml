version: 2.1

executors:
  bionic-executor-common:
    docker:
      - image: ubuntu:bionic

jobs:
  test:
    parameters:
      e:
        type: executor
    executor: << parameters.e >>
    steps:
      - run: echo "Success"

workflows:
  workflow:
    jobs:
      - test:
          e: bionic-executor-common
